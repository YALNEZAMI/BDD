prerequis:

verifier que le serveur postgres est démarré sur le port 5432
verifier que le serveur monetdb est démarré sur le port 50000

une base de donnée de créée à mettre dans config de monetdb(pas de creation automatique si db n'exist pas)
nodejs et npm
monetdb client dans PATH
set passwords dans config.js
